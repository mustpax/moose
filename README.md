# GitHub Pages repo for JS code snippets

Build commands

- `make`: build HTML files from Jade
- `make watch`: continuously build HTML files from Jade
- `make deploy`: push `dist/` folder to `gh-pages`
